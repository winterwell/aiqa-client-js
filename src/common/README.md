This folder `common` is shared by webapp, server, and aiqa-client-js.
It is for type and utility code that can be shared, to avoid repetition and improve consistency.
Be careful when editing - it must work for all of those.