Warning: .schema.json files are auto-generated by `pnpm run schema`
Do not edit them manually.
With the exception of the Span and Example types, which _are_ maintained manually.

# Data type fields use camelCase (and single-word where possible)

Our data types and API use camelCase for consistency with JSON conventions.

Single-word names are used where possible to reduce snake_case vs camelCase difference, e.g. trace (not traceId), start/end (not startTime/endTime).

